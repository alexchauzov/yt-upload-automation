# YouTube Uploader Configuration Example
# Copy this file to .env and customize the values as needed
# WARNING: Never commit the actual .env file to version control!

# YouTube API OAuth Scopes
# Multiple scopes can be separated by commas or spaces
# Default scope allows video upload
YT_SCOPES="https://www.googleapis.com/auth/youtube.upload"

# Path to YouTube API credentials file (OAuth 2.0 Client ID)
# Download this from Google Cloud Console
YT_CREDENTIALS_FILE="credentials.json"

# Path to OAuth token storage file
# This file is created automatically after first authentication
YT_TOKEN_FILE="token.json"
