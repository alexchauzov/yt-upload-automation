# Test Environment Variables for Acceptance Tests
# Copy this file to .env (or set variables directly) to run acceptance tests locally

# Google Service Account Credentials (REQUIRED for acceptance tests)
# Path to the service account JSON file
# The service account must have read access to the spreadsheets
GOOGLE_APPLICATION_CREDENTIALS=service_account.json

# Google Sheets Configuration (REQUIRED for acceptance tests)
# Template spreadsheet (read-only, contains test data setup)
TEMPLATE_SPREADSHEET_ID=your_template_spreadsheet_id_here

# Runtime spreadsheet (will be reset from template before each test run)
RUNTIME_SPREADSHEET_ID=your_runtime_spreadsheet_id_here

# Video File Workflow Directories (optional, defaults used if not set)
# Used by file workflow acceptance tests
VIDEO_WATCH_DIR=.test_data/watch
VIDEO_IN_PROGRESS_DIR=.test_data/in_progress
VIDEO_UPLOADED_DIR=.test_data/uploaded
